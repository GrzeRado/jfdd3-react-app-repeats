import React from 'react'

const UsersList = props => (
    <div>
        {
            props.usersData
            &&
            props.usersData.map(user => (

            ))
        }
    </div>
)


export default UsersList